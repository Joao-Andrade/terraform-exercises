# Local setup

This folder contains examples of terraform code to install on a local environment.

This assumes the user has installed:
 - docker
 - kubernetes
 - helm
 - kind (https://kind.sigs.k8s.io/)

The setup I used is a Macbook Pro M1 with 16gb Ram.